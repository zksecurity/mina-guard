{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/preload.js dist/ && mkdir -p dist/auro && cp src/auro/page.html dist/auro/",
    "build:ui": "cd ../ui && next build",
    "dev": "tsc -p tsconfig.json && cp src/preload.js dist/ && mkdir -p dist/auro && cp src/auro/page.html dist/auro/ && electron --no-sandbox ."
  },
  "dependencies": {
    "next": "^14.2.0",
    "ui": "workspace:*"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "typescript": "^5.8.2"
  }
}
