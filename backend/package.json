{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:push": "prisma db push --skip-generate",
    "dev": "bun run prisma:generate && bun run db:push && bun --env-file=.env --watch src/server.ts",
    "build": "bun run prisma:generate && tsc -p tsconfig.json && cp -R src/generated dist/generated",
    "start": "bun run db:push && bun dist/server.js",
    "prisma:generate": "prisma generate",
    "prisma:generate:sqlite": "node scripts/check-schema-sync.mjs && prisma generate --schema=prisma/schema.sqlite.prisma",
    "prisma:schema-sql": "node scripts/check-schema-sync.mjs && DATABASE_URL=file:/tmp/mg-schema-dummy.db prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.sqlite.prisma --script",
    "prisma:check-schema-sync": "node scripts/check-schema-sync.mjs",
    "prisma:migrate": "prisma migrate dev",
    "prisma:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "6.12.0",
    "contracts": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "o1js": "https://pkg.pr.new/o1-labs/o1js@2701",
    "zod": "^3.25"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "prisma": "6.12.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}